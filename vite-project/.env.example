# Copy to .env and set the backend URL for production builds
# For example, when backend is on Render:
# VITE_BACKEND_URL=https://your-backend.onrender.com

# Local development (Vite uses .env, .env.local, etc.)
# VITE_BACKEND_URL=http://localhost:5000
